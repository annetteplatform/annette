play.application.loader = biz.lobachev.annette.application.impl.ApplicationServiceLoader
play.http.secret.key = ${?SECRET_KEY}
play.server.pidfile.path = "/dev/null"

akka.cluster.sharding.state-store-mode = ddata

elastic {
  url = "https://localhost:9200"
  url = ${?ELASTIC_URL}
  prefix = "dev"
  prefix = ${?ELASTIC_PREFIX}
  username = "admin"
  username = ${?ELASTIC_USERNAME}
  password = "admin"
  password = ${?ELASTIC_PASSWORD}
  allowInsecure = true
  password = ${?ELASTIC_ALLOW_INSECURE}
}

cassandra.default {
  ## list the contact points here
  contact-points = "localhost:9042"
  contact-points = ${?CASSANDRA_URL}
  ## keyspace name
  keyspace = ${?KEYSPACE_PREFIX}application
  keyspace = ${?KEYSPACE}
  ## authentication
  authentication {
    username = "cassandra"
    username = ${?CASSANDRA_USERNAME}
    password = "cassandra"
    password = ${?CASSANDRA_PASSWORD}
  }

  ## override Lagom’s ServiceLocator-based ConfigSessionProvider
  session-provider = akka.persistence.cassandra.ConfigSessionProvider
  replication-factor = ${?CASSANDRA_REPLICATION_FACTOR}
}

cassandra-journal = ${cassandra.default}
cassandra-snapshot-store = ${cassandra.default}

lagom.persistence {
  read-side.cassandra = ${cassandra.default}
  snapshot-after = 100
  passivate-after-idle-timeout = 300s
}


