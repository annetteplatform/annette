include "application.conf"

# Pekko Management + Kubernetes discovery (slice 002 scaffold).
# Per dev/migration/002-build-and-gateway-foundations.md step 6.
# Slice 013 actually enables these once Lagom is removed.
pekko.management.cluster.bootstrap {
  # populated in slice 013
}
pekko.discovery.method = kubernetes-api
