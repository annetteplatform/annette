include "application.dev.conf"

# Slice 004: renamed akka.* → pekko.*. ActorSystem name "authorization" (was "application").
# Artery port 17363 per slice 002 dev-mode table.
pekko {
  remote.artery.canonical.hostname = "127.0.0.1"
  remote.artery.canonical.port = 17363
  cluster.seed-nodes = ["pekko://authorization@127.0.0.1:17363"]
}

# Per D3: lagom.circuit-breaker block dropped entirely (no replacement).
annette.http.host = "0.0.0.0"
annette.http.port = 9000
