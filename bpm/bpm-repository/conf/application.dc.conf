include "application.dev.conf"

# Standalone service (no Pekko cluster — dev/migration/012): only the bind
# address differs in docker-compose, where the container exposes 9000
# (deploy/docker/docker-compose.yml maps host 8513 -> container 9000).
annette.http.host = "0.0.0.0"
annette.http.port = 9000
