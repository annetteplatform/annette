include "application.conf"

# Dev-mode fixed ports (D2). Per dev/migration/002-build-and-gateway-foundations.md step 7.
# Slice 012 onward binds the service's own Http().newServerAt(...) to annette.http.port.
# No artery/cluster config: the service runs standalone (local actor system).
annette.http.port = 8513
