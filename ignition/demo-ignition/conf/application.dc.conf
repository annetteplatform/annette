include "application.conf"

# gRPC endpoints come from application.conf (dev-mode ports); Keycloak stays external.
annette.ignition.keycloak.url = "http://localhost:8080"
annette.ignition.keycloak.url = ${?KEYCLOAK_URL}

